{
  "name": "08-mongodb-intro",
  "version": "1.0.0",
  "scripts": {
    "seed": "node src/db/seed.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": { "testEnvironment": "node" }
}
